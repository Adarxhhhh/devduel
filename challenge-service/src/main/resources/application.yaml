server:
  port: 8082

spring:
  application:
    name: challenge-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/challenge_db}
    username: ${DB_USERNAME:adarshkatigi}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

judge0:
  url: ${JUDGE0_URL:https://judge0-ce.p.rapidapi.com}
  api-key: ${JUDGE0_API_KEY:98975bef2fmsh4c76d7986ed30f8p17a984jsn95b7c4cadc97}
